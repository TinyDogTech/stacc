//! Stack operations — the orchestration layer that ties the other crates
//! together to implement `submit`, `sync`, and `restack`.
//!
//! See `plans/algorithms.md` (Squash-merge detection, Restack, Conflict resume).
